THE DEN - WORKING CONTEXT
=========================
Kept up to date by Claude at every milestone so a fresh session can resume without
the chat history. Newest entries at the top of each section. Last updated: 2026-09-13.

PICK UP HERE (2026-09-13 evening: the project moved to Liam's HoltOS laptop, Linux)
-------------------------------------------------------------------------------------------
Work no longer happens on the Windows machine (C:\projects, WSL Ubuntu). The three repos were cloned fresh from
GitHub into /home/liam/Projects/theden/the-den, the-den-client and the-den-android on Liam's HoltOS 0.0.4-alpha
laptop (Arch based, KDE Plasma on Wayland). The paths, venvs and scripts in the older sections below describe the
Windows machine and have not been re-verified here.

WHAT THIS MACHINE HAS (checked 2026-09-13):
  - Python 3.14.7, libtorrent-rasterbar 2.1.1 (the Python bindings import), chromium, sqlite, PySide6 6.11.2,
    Kirigami, qqc2-desktop-style, ttf-nunito, ttf-jetbrains-mono, yay, and /dev/kvm for an Android emulator.
  - Missing: a the-den dev venv, JDK 17, the Android SDK and adb. jdk17-openjdk and android-tools are in the extra
    repo and need Liam's sudo. zeroconf, acme and webauthn go into the venv for M34, M35 and M38 (webauthn is not
    in the Arch repos).
  - HoltOS installed The Den v0.7.0 as the-den.service on 127.0.0.1:8686 (/opt/the-den, database under
    /var/lib/the-den) and the KDE client v0.4.16 in /opt/the-den-client. Nobody has finished its /setup page yet.

LOST IN THE MOVE (these only existed on the Windows machine and were never committed):
  - app/discovery.py, the M34 draft. M34 has to write it again.
  - The 72-check e2e (scratchpad m11g/full.sh), start_dev_plex.sh and the other scratchpad helper scripts.
  - tests/e2e_notifications.sh still hardcodes /mnt/c/projects/theden and ~/.venvs/the-den.

URGENT BUG FOUND ON THE LIVE INSTALL: app/torrent/engine.py reads torrent_status.paused (lines 303 and 438) and
  torrent_status.auto_managed (line 308). libtorrent 2.1.1 removed both attributes, so the engine loop raises
  AttributeError about once a second (about 10,000 times in the first two hours after boot), seed limits never run,
  and /ui/downloads returns 500. The old WSL box had Ubuntu's libtorrent 2.0, which still had them. Fix: test
  st.flags against lt.torrent_flags.paused and lt.torrent_flags.auto_managed instead. Verify with
  tests/local_swarm.py on libtorrent 2.1.1.

NEXT, in order:
  1. Set up this machine. PARTLY DONE 2026-09-13.
     Done: the-den venv (~/.venvs/the-den), the dev backend on 8687 with /setup finished by Liam (admin account
     livetest; the password is Liam's and is not recorded anywhere), Liam's admin API token saved by Liam in
     ~/the-den-dev/admin-token.txt (mode 600; Claude never writes tokens), and the KDE tests/run_all.sh at 12/12 against
     the-den-client's new tests/fixture_backend.sh on 8688 (see DEV ENVIRONMENT below).
     Still open: Liam picks a backup folder for the git bundles (none written yet); Liam runs
     `sudo pacman -S --needed jdk17-openjdk android-tools`; install the Android SDK cmdline-tools (platform 35,
     build-tools 35.0.0) under ~/Android/Sdk; rebuild the lost 72-check e2e and the mock Plex starter inside tests/;
     update README and HANDOFF paths; fix the-den-client's tests/qml_harness.py, which rewrites the tracked
     tests/_shell.qml with this machine's absolute path on every run (restore it with git checkout before committing).
     Liam should revoke the admin token (it was pasted into chat) and save a new one; rerun tests/fixture_backend.sh
     afterwards, since it copies users and tokens from the dev database.
  2. (DONE 2026-09-13) The libtorrent 2.1 fix: engine.py now reads
     torrent_status.flags for paused and auto_managed, and drops torrent_status.states.allocating, a fourth break the
     live install never reached. Verified 11/11 on libtorrent 2.1.1 against tests/local_swarm.py: .torrent and magnet
     downloads, pause, resume, queued, seed limits enforced by the background loop, done, restart restore, and no loop
     errors. RELEASED 2026-09-13 with M33 as the-den v0.8.0a (e9104bd), alongside the-den-client v0.5.0a
     (e29b06a). Both tags are pushed.
     INSTALLED STATE (checked 2026-09-13 17:40): the updater installed v0.8.0a at 17:15 but history.log says
     "v0.8.0a (FAILED to start)". That was the old updater, from before the HoltOS port fix; the cause is unknown
     because the laptop rebooted at 17:23 and the journal only keeps the current boot. Since the reboot /opt/the-den runs
     the v0.8.0a code on 127.0.0.1:8686 with no engine errors, but its /setup page is not finished yet (Liam). The
     client v0.5.0a installed cleanly at 17:15.
  3. M34 (docs/connect-secure-plan.md), NEXT UP, ships as the-den v0.8.1a: rewrite app/discovery.py, make
     deploy/the-den.service use WEB_HOST and WEB_PORT instead of the hardcoded 127.0.0.1:8686, and add both keys to
     deploy/the-den.env.example. Nothing of M34 is written or pushed yet; app/config.py only has the 40204 default.
  4. M37 device tokens can go before M35 while Liam answers the router questions (ports 80 and 443, NAT loopback,
     device approval).
  5. M35 HTTPS, M36 UPnP, M38 passkeys, M39 Android, M40 KDE, then the M14 shakedown, Tier 3 (M41-M47) and M48
     Cloudflare DNS.

LIVE TESTING THROUGH THE HOLTOS UPDATER (Liam, 2026-09-13): push a tagged release of the-den or the-den-client and
  the HoltOS updater (/usr/local/bin/holtos-update-apply) installs the newest v* tag here from the GitHub tarball. For
  the server it installs the pip requirements, runs alembic upgrade head, restarts the service and waits for
  http://127.0.0.1:8686. /etc/holtos/updates.conf has AUTO_INSTALL=apps, so the updater's 6-hour timer installs any new
  tag without asking: only tag tested work. Android is not covered and is installed over adb.
  Versioning (Liam): small jumps, a letter added to the version already in the repo files, starting with the-den
  v0.8.0a, client v0.5.0a and Android versionName 0.7.0a with versionCode 23, then b, c and so on. Git's version sort
  ranks v0.8.0a above v0.8.0 but pacman's vercmp ranks it below, so never tag the plain base once letters exist (go
  to v0.8.1a). After z, move to the next patch number, because aa sorts before b. No -rc1 or .1 suffixes. The-den's
  PKGBUILD pkgver still says 0.7.0.
  HoltOS port fix (LANDED in HoltOS v0.0.6a-alpha, installed here 2026-09-13 17:36): holtos-update-apply now reads
  WEB_HOST and WEB_PORT from /etc/the-den/the-den.env (port 8686 when unset), probes /health over http and then https
  with -k, and rewrites the-den.desktop on every update, so M34's port move no longer reports a false failure.
  Follow-up bug, FIXED in HoltOS v0.0.6b-alpha (e70999f): the updater's self-handover ran
  `holtos-update-apply config <tag>` and the tag was read as an unknown item, so every handed-over config update
  ended "Failed" and ignored the tag. A tag right after config is now that item's target. v0.0.6b-alpha was
  published but not yet installed on the laptop at the time of writing.
  GitHub sign-in: the gh token expired on 2026-09-13 and Liam signed in again; git pushes use that login. If a push
  or fetch fails with "could not read Username", ask Liam to run gh auth login.

LOCAL MODELS ON THIS MACHINE: two user-scope MCP servers in ~/.claude.json run ~/.claude/mcp-servers/lmstudio_mcp.py
  (stdlib only; tools list_models, chat and run_coding_task; writes are sandboxed to /home/liam/Projects; the result
  includes a real unified diff). lmstudio-bridge is the 4090 on LiamPC at http://192.168.0.152:1234
  (qwen2.5-coder-32b-instruct at 14,592 context, which prints tool calls as text that the bridge parses;
  qwen3-coder-30b-a3b also worked when loaded). lmstudio-4080super is http://192.168.0.39:1234 (qwen/qwen3.5-9b at
  92,672 context; use it for big files like this one). Machine-wide rules for every session are in
  ~/.claude/CLAUDE.md.

DEV ENVIRONMENT ON THIS MACHINE (set up 2026-09-13):
  - Venv: ~/.venvs/the-den, made with python -m venv --system-site-packages so it sees the system libtorrent 2.1.1,
    then pip install -r requirements.txt.
  - Dev backend: 127.0.0.1:8687, data in ~/the-den-dev (den.db, state, downloads, movies, tv), torrent port 6893,
    log ~/the-den-dev/server.log.
  - Test backend: 127.0.0.1:8688 is the KDE client's fixture backend, built by the-den-client's
    tests/fixture_backend.sh: a copy of the dev database (for its users and tokens) seeded with movie 1, series 1
    and a mock indexer, backed by mock TVmaze on 8085 and mock Torznab on 8082. Data and logs are in ~/the-den-test,
    which each run wipes; torrent port 6894. The KDE tests/run_all.sh is 12/12 against it.
  - Start each from the repo root with DATABASE_URL, STATE_DIR, DOWNLOADS_ROOT, MOVIES_ROOT, TV_ROOT, TORRENT_PORT,
    WEB_HOST and WEB_PORT exported, run ~/.venvs/the-den/bin/python -m alembic upgrade head, then
    setsid nohup ~/.venvs/the-den/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port <port>.
    Neither has finished /setup yet; Liam does that and makes an admin token.
  - Local swarm: SWARM_DIR=~/the-den-swarm ~/.venvs/the-den/bin/python -m uvicorn tests.local_swarm:app --port 8083,
    seeding on 6891. Releases are numbered from 1 (/download/1.torrent, /magnet/1).
  - Ports already taken: 8686 and 6881 by the HoltOS-installed copy, 8083 and 6891 by the swarm, 8085 and 8082 by
    the fixture mocks.
  - Offline tests pass here: tests/test_formats.py, tests/test_importer.py and tests/test_native_indexers.py.
  - Gotcha: from app.torrent import engine gives the shared TorrentEngine instance, not the module. Scripts that
    need EngineConfig or TorrentEngine should import the module and use sys.modules["app.torrent.engine"].

PREVIOUS PICK UP HERE (2026-09-13, Tier 2 complete: E1-E10 all done and pushed)
-------------------------------------------------------------------------------------------
Tier 2 (docs/feature-research.md, E1-E10) is finished; E10 (backup/restore, M32) was the last item,
see its DONE entry below. feature-research.md's summary now notes that everything except the N9
real-world shakedown has shipped.

NEXT, in order:
  0. The next body of work is docs/connect-secure-plan.md (M33 to M48, planned 2026-09-13 at Liam's request):
     always-on sign-in with a first-run setup, server identity and LAN discovery, HTTPS, UPnP remote access via
     requesthome.asuscomm.com (Cloudflare DNS later), device tokens, passkeys, the Android and KDE find /
     remember / switch work, then the Tier 3 queue. Server port: 40204 (Liam). Commits: commit each verified
     milestone locally and do NOT push until Liam says so (Liam, 2026-09-13; this overrides the push rule below
     for this work), and write a git bundle of each repo outside the working trees after every commit.
     Progress: M33 done and pushed 2026-09-13 (Liam said push). Next is M34: review app/config.py's WEB_HOST/WEB_PORT/
     SERVER_NAME (committed with M33, unused so far) and app/discovery.py (uncommitted draft; it still calls the removed
     auth.required(), drop its "auth" TXT record).
  1. (DONE 2026-09-13) E6 gap closed: subtitle settings on Android 0.6.8 and KDE 0.4.16, see the DONE
     entry "E6 GAP" below.
  2. (DONE 2026-09-13) Plex bug: /api/plex/servers and /api/plex/sections 500 on a rejected token --
     fixed, see the DONE entry "PLEX 401 FIX" below.
  3. Android: nobody has looked at E9's Notifications section or E6's Subtitles section on a real screen
     yet. The 0.6.8 debug APK is installed on the Pixel 9 Pro Fold (in place, app data kept). The dev
     backend runs on 8686 (DB migrated to head; pre-migration copy ~/the-den-dev/den.db.pre-e10-upgrade,
     log server-e10.log) and the Windows portproxy points at the current WSL IP. Needs Liam: the phone is
     locked, and its saved API token may predate the rebuilt dev DB (owner-token.txt is gone).
  4. M14 shakedown on the HoltOS box is Liam's. Tagging a release waits for his "push and tag" (last
     tagged 2026-09-13: the-den v0.7.0, client v0.4.16, android v0.6.8; PKGBUILD pkgver now matches).

STANDING RULE (updated 2026-09-13 evening; supersedes every older push rule in this file): commit each
  verified milestone locally and do not push until Liam says so. The one exception is a tagged release pushed for
  live testing through the HoltOS updater (see PICK UP HERE). After every commit, write a git bundle of each repo
  outside the working trees once Liam picks the backup folder. Git now runs natively on Linux, so the old WSL
  credential-prompt hang no longer applies.

BRIDGE NOTE (2026-09-13, corrected later the same day): run_coding_task never wrote a file during
  E9's finish because the model then loaded on the 4090, qwen2.5-coder-32b-instruct, does not make
  tool calls -- LM Studio's server log shows it answering every tool_choice=required request with an
  empty tool_calls list. The "Request timed out" errors were a symptom: run_coding_task makes a
  blocking requests.post inside an async tool, so a slow generation also stalls health_check. Fix:
  use a tool-capable model. Tested working afterwards: 4090 qwen3-coder-30b-a3b-instruct and 4080
  qwen/qwen3.5-9b both pass a verified read_file probe. Quirks: the 4080 bridge's get_current_model
  returns 400 even with a model loaded (don't treat that as "no model"); qwen3.5-9b reasons before
  answering, so small max_tokens gives an empty reply; the 4090 miscounted a list_dir result once,
  so verify what a worker reports.
  What Claude wrote directly for E9's finish, stated precisely (an earlier version of this note
  overstated the exception): ApiClient.kt and SessionViewModel.kt after two failed handoffs each;
  AdminScreens.kt after one; TheDenApi.kt (via the bridge's edit_file tool, a plain find-and-replace
  with no model involved), MainActivity.kt, build.gradle.kts, ROADMAP.md, feature-research.md and
  this file with no handoff attempted. Commits 8d7498e (the-den) and 154a794 (the-den-android) carry
  the overstated wording; this note is the correct record. The follow-up sign-in gate on the Android
  Notifications section was written by the local model.

REPOS AND VERSIONS
------------------
- the-den            C:\projects\theden             github.com/jamesyoungdahr-debug/the-den        v0.7.0 tagged (ff5fc9d); master now carries M33, untagged, on the 0.8.0 track (stays 0.x -- see "no 1.0.0 yet" below)
- the-den-client     C:\projects\the-den-client     (KDE, PySide6/Kirigami)                         v0.5.0 (M33, untagged; v0.4.16 tagged at f410c6f)
- the-den-android    C:\projects\the-den-android    (Kotlin/Compose)                                v0.7.0 (M33, untagged; v0.6.8 tagged at 4de2bfe)
- Rule: the-den and the-den-client stay SEPARATE repos and SEPARATE packages; the HoltOS updater tracks both.
- Git identity for commits: user.name Liam, user.email jamesyoungdahr@gmail.com (set locally in all three repos).

WHAT THE DEN IS
---------------
FastAPI + SQLAlchemy/Alembic (SQLite) + Jinja2 + APScheduler media manager with a built-in
libtorrent client, Discover (TMDB), Plex login + library scan, Requests with quotas, and the
HoltOS Glass UI. Companion apps: KDE client (the-den-client) and Android app (the-den-android).

LAST TASK: M22 - DISCOVER GROUPED (MOVIES / SERIES) AND PAGED "VIEW MORE" (2026-09-13, done, tagged). the-den v0.6.7, client v0.4.11, android v0.6.1. Tier 1 plan:
  docs/tier1-plan.md; Tier 2 candidates: docs/feature-research.md (E1-E10). M22 was the first post-Tier-1 item (Liam's request), rails in
  app/routers/discover.py RAILS, tmdb.* take page=.
DONE 2026-09-13: M33 - SIGN-IN ALWAYS REQUIRED, FIRST-RUN SETUP (DONE; server f777a75, client 0.5.0, android 0.7.0; untagged).
  Server: no anonymous admin and no AUTH_REQUIRED; app/auth.py's guards always answer 401 or send to /login.
  app/setup_state.py plus a gate in app/main.py's middleware serve only /health, /setup*, /login*, /logout and static
  files until an admin exists and setup step 2 (/setup/server: server name + library folders) is saved; browsers go to
  /setup, apps get 503 "setup required". /health gains setup_complete. Migration f9a0b1c2d3e4 drops settings.auth_required
  and adds server_name and setup_completed_at. Settings > Accounts is gone. Existing installs finish step 2 once.
  Clients: the anonymous paths are gone and both show "This server hasn't been set up yet" for an unfinished server.
  Claude wrote the security units (auth.py, setup_state.py, the gate, routers/auth.py, the sign-in switch removals in
  plex.py/api_settings.py/ui.py) and the Android AdminScreens.kt brace fix after two failed handoffs; the local models
  wrote everything else. Verified: a 41-check server smoke (migration round-trip, fresh-install gate, both setup steps,
  401s, token auth, an upgraded install finished only by its admin), the notifications e2e with a token (test DB migrated,
  backup ~/the-den-test/den.db.pre-m33, admin token in ~/the-den-test/e2e-token.txt), KDE run_all.sh green plus a headless
  setup-required check, Android build and unit tests 10/10. Not checked: Android's setup-required message on a real screen.
DONE 2026-09-13: E6 GAP - SUBTITLE SETTINGS ON KDE AND ANDROID (DONE). Closes the gap E6 left (its surfaces column asked
  for settings on every surface; only the web had them). Android 0.6.8 (commit 27a927d): a Subtitles section on Settings
  (OpenSubtitles API key as a secret + languages); SettingsViewModel only sends the key when typed. KDE 0.4.16 (commit
  526bfdf): a Subtitles section on SettingsPage.qml, the key added to settings_controller.SECRETS and cleared after a
  save. Both STATUS.md files got entries (Android also got the missing 0.6.7 one). Written by the local models from
  exact specs, reviewed per diff. Verified: Android assembleDebug + unit tests 10/10; KDE QML harness clean on
  SettingsPage.qml and check_settings_controller.py passes (both against a throwaway server on 8687). Not yet seen
  on a real screen.
DONE 2026-09-13: PLEX 401 FIX (server only). app/plex.py list_servers now raises PlexError instead of letting httpx
  errors escape: a 401 gives "Plex rejected the saved token; reconnect the Plex account in Settings", other HTTP or
  connection failures and an unreadable body are wrapped too. app/routers/plex.py api_sections now catches PlexError
  around list_servers (api_servers already did), so both routes return a 502 with a JSON detail the Settings page
  shows. No other callers of list_servers exist (plex_scan uses library_sections; Plex login uses get_account).
  Verified on a throwaway copy of the test DB against tests/mock_plex.py on 8084: bad token -> JSON 502 on both
  routes, good token -> 200 servers + sections, plex.tv unreachable -> JSON 502, no tracebacks in the log.
  Claude wrote both code edits itself after two failed run_coding_task handoffs on each file (the 4090 model hit
  max_iterations, claimed it had no access to C:\projects, and once wrote a placeholder file into the wrong repo,
  C:\Users\Liam\OpenJarvis\src\openjarvis\connectors\plex.py -- untracked; deleted with Liam's OK). The 4090 model drafted
  this entry but put it at the end of the file; after two failed fix rounds (4090 empty reply, 4080 timeout) Claude
  moved it here. Commit dc2ee00 touched only those two files; E10 was committed separately in 33edfc7.
DONE 2026-09-13: E10 - BACKUP AND RESTORE (DONE, M32, server + web only per feature-research.md's surfaces column).
  New app/backup.py: snapshot() copies the live database with SQLite's backup API (consistent while the app writes);
  validate() checks an upload (SQLite header, PRAGMA integrity_check, the alembic_version/settings/users/indexers
  tables, and a schema revision equal to this install's alembic head -- older backups are refused, not migrated);
  stage_restore() writes it beside the live DB as den.db.restore-pending; apply_pending_restore() runs as the first
  startup hook in app/main.py, keeps the old DB (and any rollback journal) as den.db.pre-restore-<timestamp>, and
  swaps the staged copy in; indexers_export() lists indexers without api_key. New app/routers/backup.py (page_admin):
  GET /ui/backup/database, /ui/backup/settings.json (reuses api_settings.get_settings, so secrets stay masked),
  /ui/backup/indexers.json, POST /ui/backup/restore (multipart upload + confirm=yes), POST /ui/backup/restore/cancel.
  Web: a Backup panel on Settings (nav link, downloads, upload form, or a "restore staged" state with Cancel);
  ui_settings passes restore_pending. Restore needs a restart on purpose: the scheduler and torrent engine hold the
  live DB, and systemd's Restart=on-failure won't bring a clean exit back. All E10 code and docs were written by the
  local models (4090 qwen3-coder-30b-a3b-instruct, 4080 qwen/qwen3.5-9b) from exact specs; Claude reviewed each
  diff. Verified: app.main imports; a DB-level smoke test (20/20: snapshot, every validate refusal, stage/cancel,
  swap-and-keep, export has no api_key); a live HTTP test on a throwaway DB copy (21/21, including a real restart
  that applied a staged restore); a browser pass of the panel, the staged state and Cancel.
DONE 2026-09-13: E9 - PER-USER NTFY TOPIC (DONE, M31, server + web + Android per feature-research.md's surfaces column --
  no KDE for this one). New User.notify_ntfy_topic column (migration e8f9a0b1c2d3), notifier.notify_user() (a personal
  ntfy.sh push, alongside not instead of the admin-configured agents
  notify_event() already fans out to; no-ops cleanly with no topic or no user), wired into requests_service's three
  requester-facing outcomes (mark_available/approve/decline) -- deliberately NOT request_submitted, since that event is
  admin-facing. Server API: _user_out() exposes notify_ntfy_topic, new POST /ui/profile/notifications (web) and POST
  /api/auth/notifications (JSON, Android) routes. Web: a Notifications panel on the profile page, following the existing
  password panel's holt-panel + form pattern. Android: TheDenApi.kt's MeResponse/Session gain notifyNtfyTopic, a new
  NotificationSettingsBody + updateNotifications() endpoint, SessionViewModel gains refresh()/saveNotifyTopic(), and a
  new AccountNotificationsSection composable sits under the account header on the "More" tab -- distinct from the
  existing PushSection (that's this device's own push subscription; this is the server's per-user setting).
  This session resumed a prior session's pause: the code was already written and uncommitted (commit b668c9c, WIP/
  untested) when picked up. Verified before commit: migration round-trip (upgrade/downgrade -1/upgrade) against the
  test DB; `python -c "import app.main"` clean; a live HTTP pass against the restarted test server covering both the
  HTML form (/ui/profile/notifications) and the JSON route (/api/auth/notifications), including the empty-string-clears
  -to-null case; a DB-level smoke test of notify_user() (no topic -> no-op, no user -> no-op, topic set -> send() called
  with the right kind/config/event/message/link, and a raised exception from send() is swallowed); Android assembleDebug
  BUILD SUCCESSFUL and testDebugUnitTest 10/10 (LiveBackendTest 5/5, PushTest 5/5). See the BRIDGE NOTE above the DONE
  log for why this session's Android/docs edits were written directly rather than through lmstudio-bridge.
DONE 2026-09-13: M23 - MANUAL IMPORT (DONE, all three surfaces). the-den v0.6.9, client v0.4.12, android v0.6.2. See ROADMAP.md for the summary.
DONE 2026-09-13: E5 - PER-TITLE HISTORY (DONE, all three surfaces). New `history_events` table (migration e2f3a4b5c6d7) and app/history.py
  (record/for_movie/for_episode/for_series/as_dicts). Recording added at every lifecycle point: grabber._grab ("grabbed"/"upgraded"), download_check._import
  ("imported"/"upgraded"), download_check.fail_download ("download_failed"), download_check.reap_seeded and routers/torrents.py remove_torrent ("removed",
  only when the torrent had a title). discover.py's _detail() (used by the web page and the JSON API both clients share) now returns `item["history"]`, and
  discover_detail.html shows a History panel. Verified: e5_smoke.py (DB-level, full grab->import->fail->remove->detail pipeline, 5/5), e5_web_smoke.sh (live
  detail page and JSON API both carry history). KDE's DetailPage.qml reads page.item.history directly (no model change needed, JsonRecord passes the raw
  JSON through) and is verified clean via the QML harness. Android's TheDenApi.kt gained a HistoryEntry data class and DetailResponse.history, and
  DetailScreen.kt gets a History GlassPanel with a tone-mapped Badge per event; verified with a debug build. the-den (commit 0a8d9b4, untagged), client
  v0.4.13, android v0.6.3.
DONE 2026-09-13: E6 - SUBTITLES (DONE, server only -- documentation gap fixed 2026-09-13: this was implemented and pushed in the same commit as E1/
  E2/E4 during the rm -rf recovery, but never got a DONE entry here or in ROADMAP.md/feature-research.md at the time; code has been live and working
  since commit 582763e). New Settings.opensubtitles_api_key/subtitle_languages; app/opensubtitles.py is a thin OpenSubtitles REST v1 client (search +
  download by TMDB id); app/subtitles.py's fetch_missing() writes missing-language .srt files as siblings of each movie/episode's video file (Plex's
  own convention, e.g. `Movie (Year).en.srt`), called as a best-effort step at the end of every automation cycle. A Subtitles panel on web Settings
  (API key + comma-separated language list). No KDE/Android UI was built for this (feature-research.md's surfaces column says "settings on all
  surfaces", so this is a real scope gap, not a documented cut like E4/E7/E8's KDE/Android omissions -- worth doing if picked back up). Verified via
  a DB-level smoke test with mocked OpenSubtitles calls (no live API key available in this environment): no-key no-op, per-language dedupe (a
  pre-existing language's file is left alone), correct sibling filename, idempotent re-runs.
DONE 2026-09-13: E1 - IMPORT LISTS (DONE, all three surfaces). New `import_lists` table (migration f3a4b5c6d7e8, mirrors NotificationAgent's shape) and
  app/import_lists.py (sync_one/sync_all), synced on a timer via a new scheduler job (settings.import_list_interval_minutes, default 360 min).
  tmdb.get_list() adds v3-list fetching (movies only); plex.watchlist() reads the connected owner account's Discover watchlist. Requests approval logic in
  requests_service.py was refactored into reusable get_or_create_movie/get_or_create_series, now shared with import_lists.sync_one so a list add goes
  through the same path as approving a request. New /api/import-lists CRUD + manual sync endpoint; an Import Lists panel on web Settings (mirrors the
  Notifications panel). Verified: migration round-trip, a DB-level smoke test (both kinds, dedupe, unknown kind, TVmaze-mapping failure), a live HTTP
  smoke test of the full API, and a live browser pass with a real sync against TMDB. KDE gained an ImportListsModel and an Import Lists Section/dialog on
  SettingsPage.qml, verified via the QML harness (15 pages clean); client v0.4.14. Android's TheDenApi.kt gained ImportList/ImportListRequest and the
  CRUD+sync endpoints, ImportListsViewModel mirrors NotificationsViewModel, and ImportListsSection on the Settings screen mirrors NotificationsSection;
  verified with a debug build. android v0.6.4.
DONE 2026-09-13: E2 - ROOT FOLDERS (DONE, all three surfaces). New `root_folders` table (name, media_type, path, is_default) plus a nullable
  root_folder_id FK on Movie and Series (migration a4b5c6d7e8f9). app/root_folders.py's root_folder_for() resolves a title's own folder, else the
  media type's marked default, else the existing global Settings.movies_root/tv_root -- mirroring candidates.profile_for()'s fallback pattern, so an
  install with zero root folders behaves exactly as before. Wired into every import call site that previously hardcoded s.movies_root/s.tv_root
  (download_check.py's three branches, routers/downloads.py's manual assign endpoint). New /api/root-folders CRUD (only one default per media type).
  No per-title root-folder picker UI exists yet on any surface (matches the same gap quality profiles already have) -- scoped out, noted in
  feature-research.md. KDE gained a RootFoldersModel and a Root Folders Section/dialog on SettingsPage.qml, verified via the QML harness (15 pages
  clean); client v0.4.15. Android's TheDenApi.kt gained RootFolder/RootFolderRequest and the CRUD endpoints, RootFoldersViewModel mirrors
  ImportListsViewModel, RootFoldersSection mirrors ImportListsSection; verified with a debug build. android v0.6.5.
DONE 2026-09-13: E4 - RENAME EXISTING FILES (DONE, server + web only, per feature-research.md's surfaces column -- no KDE/Android for this one).
  app/renamer.py reuses importer.py's movie_name/episode_name to diff a title's current file_path against what it should be, filenames only (no
  folder restructuring). Same-filesystem rename preserves the inode (a hardlink to a still-seeding torrent copy is unaffected); copy+delete fallback
  across filesystems. New /api/rename (preview/apply/apply-all) and a plain server-rendered /ui/rename review-then-confirm page (linked from
  Settings), since nothing else in the app had a preview/confirm flow to reuse. Verified: DB-level smoke test with real files, live HTTP smoke test,
  and a full browser pass (seeded a mismatched file, previewed, clicked Rename, confirmed the move on disk).
NOTE 2026-09-13: this repo (the-den server only) suffered an accidental `rm -rf` mid-session that deleted the whole working directory including .git,
  after E1/E2/E4 had been committed locally but before any of it was pushed. Recovered by re-cloning the last-pushed commit and reconstructing every
  file change from the diffs already verified earlier in the same session; all four milestones' smoke tests were re-run and passed identically
  post-recovery. Recorded here as a standing lesson: push to origin more often, not just at natural stopping points, so local-only commits are never
  the sole copy of a session's work for long.
DONE 2026-09-13: E7 - USENET VIA SABNZBD (DONE, server + web only, per feature-research.md's surfaces column). Scoped as integration with an
  existing SABnzbd instance (URL + API key, mirroring the FlareSolverr precedent) rather than an in-process NZB downloader -- the torrent engine
  isn't a generic abstraction and info_hash is threaded deep through the download pipeline, so building a from-scratch usenet client would have
  meant writing an article-fetch/repair/unrar pipeline with no equivalent to libtorrent to lean on. Release.protocol (torznab|newznab) flows from
  Indexer.protocol through candidates.scored_candidates and GrabRequest to every grab call site (manual and automatic). DownloadRecord gained
  download_client ("torrent"|"sabnzbd"); info_hash is reused as the generic client-key column. app/sabnzbd.py is a thin httpx client; three small
  dispatch helpers in download_check.py (_status/_files/_remove) let check_and_import/fail_download/reap_seeded work against either client without
  duplicating logic. Verified: migration round-trip, a DB-level smoke test of the full grab->check->import->reap pipeline against a mocked SABnzbd
  client, a live settings save/read round-trip, and confirmed the existing torrent path still works after the download_check.py rewrite. the-den
  commit 6d4d6fd (untagged).
DONE 2026-09-13: E8 - REQUEST COMMENTS AND A 4K FLAG (DONE, server + web + Android, per feature-research.md's surfaces column -- no KDE for this one).
  New request_comments table (mirrors HistoryEvent's append-only/FK-to-parent shape; new visibility rule -- requester + any admin, via
  _can_see_comments()). MediaRequest.is_4k is informational only (no quality-profile routing). New GET/POST /api/requests/{id}/comments; a JS-driven
  panel on web requests.html and a ModalBottomSheet on Android's RequestsScreen.kt, both new UI since no comment-thread precedent existed anywhere
  in the app. Two real bugs found and fixed during testing: the new endpoints used the HTML-route _require_me() helper (redirect) instead of the
  JSON-API 401 convention; deleting a request didn't delete its orphaned comments (SQLite doesn't cascade, and a reused autoincrement id could
  resurface them under an unrelated request) -- fixed with requests_service.delete_request(). Verified: migration round-trip, a live HTTP smoke
  test covering the full comment CRUD + both bug fixes, a live browser pass, and an Android debug build. the-den commit c45e207 (untagged), android
  v0.6.6.
TIER 1 STATUS: M15 notifications, M16 health/stats, M17 custom formats, M18 upgrades, M19 failed downloads, M20 season packs, M21 Android push are
  DONE and pushed (server 0.6.0 -> 0.6.6, client 0.4.5 -> 0.4.10, android 0.5.2 -> 0.6.0, each tagged). Remaining: M14 shakedown on the HoltOS box
  (user); no 1.0.0 planned, the server stays on 0.x (Liam, 2026-09-13). Verification so far is API smokes, fake-engine probes, the KDE harness, Android JVM tests and builds; the local-swarm
  e2e for M18-M20 (upgrade swap, seeder disappears, 3-file pack) has not been run. Android 0.6.0 APK built, not installed on the phones yet
  (wireless-debug ports change; re-pair when needed); push has not been tried end to end with a real ntfy topic on a phone.
LOCAL-MODEL WORKFLOW (user rule: all code is written by the local models, which edit files themselves via lmstudio-bridge run_coding_task; Claude
  specs, reviews the git diff, tests, commits): the 4090 runs qwen3-coder-30b-a3b, the 4080 runs essentialai/rnj-1, both at 32k context.
  Findings: the 4080 is reliable on files under ~200 lines and needs an occasional one-line fix round; the 4090 passes tiny probes and mid-size
  files but times out on many real units even after a restart. Both return nothing or time out on whole-file rewrites over ~250-300 lines
  (app/routers/ui.py, app/templates/downloads.html, TheDenApi.kt, LibraryScreens.kt, AdminScreens.kt), so those edits were made by Claude after
  the failed rounds, as recorded in each commit. Mitigations: new code goes in new files or Jinja partials; long files get only tiny targeted
  edits; one unit per machine at a time; always give an idempotence guard ("if already present, leave unchanged") because timed-out tasks can
  still land later.
  M21 finding: the long-file failures come from the bridge's coding agent having only write_file (whole-file rewrite) at max_tokens 4096. Claude
  patched C:\projects\lmstudio-mcp\lmstudio_bridge.py: edit_file tool (exact old/new replace), LMSTUDIO_TIMEOUT (default 900 s) and
  LMSTUDIO_AGENT_MAX_TOKENS (16384); MCP_TOOL_TIMEOUT=900000 in ~/.claude/settings.json. Both take effect after the MCP servers restart.
------------------------------------------------
Plan and status: the-den/docs/indexers-plan.md. All code in this task after the first pass was
drafted by the local LM Studio model through lmstudio-bridge (user rule: ALL code, even
one-liners, goes to the local model; Claude specs, reviews, tests, commits).
Server (the-den, uncommitted): app/indexers/{__init__,catalog,native,fetch,solver}.py,
migration c9d0e1f2a3b4, routers/indexers.py (+/presets, +/solver/test), search+candidates+ui
dispatch through app.indexers, settings.flaresolverr_url everywhere, web indexers dialog =
preset picker, settings page Indexers section, PKGBUILD depends on chromium, requirements
nodriver==0.46, tests/test_native_indexers.py (11 checks pass), docs/privacy-android.md.
Android (uncommitted): drawer nav + blur + permanent sidebar >=840dp, rememberSaveable state,
release signing config (keystore.properties, gitignored), preset picker in Indexers add sheet,
solver field in Settings, PRIVACY.md. Debug build installed on Pixel 9 Pro Fold + Galaxy S25.
KDE client (uncommitted): indexer_model presets/loadPresets/addIndexer(preset,...),
IndexersPage preset combo, SettingsPage solver field, check_indexer_model.py updated (passes).
DONE 2026-09-12: server cleanup (shared query/profile helpers, 8e56b1d); KDE QML harness runs in WSL again (system PySide6 6.10 + KF6 Kirigami via apt; python3, not the venv) and all 13 pages compile clean; screenshot walk renders 10 pages.
OPEN AFTER THIS TASK: KDE icon + QML + built-in solver verification on the HoltOS box, Android 0.5.1 APK not yet installed on the phones (they dropped off adb), library
header wording DONE in all three clients.
Repo status lines above (REPOS AND VERSIONS) are stale: 0.5.0 / 0.4.4 / 0.5.1 now.
VERIFIED LIVE: Knaben, TPB, Nyaa, LimeTorrents, TorrentDownloads. NOT: YTS (DNS blocked here),
1337x/EZTV via built-in solver (Cloudflare refuses this network even in a normal browser;
test on the HoltOS box).

PREVIOUS TASK: U5 - ANDROID REDESIGN (COMPLETE 2026-09-12, v0.5.0).
------------------------------------------------
Plan: the-den/docs/ui-redesign-plan.md (U5 row). Target version 0.5.0 (versionCode 5).
Done so far (all uncommitted in the-den-android):
- app/build.gradle.kts: versionCode 5 / versionName 0.5.0; deps added: material-icons-extended,
  material3-window-size-class, io.coil-kt:coil-compose:2.7.0.
- network/: ApiClient (singleton, SharedPreferences "the-den", session StateFlow), ApiClientFactory
  (OkHttp, X-Api-Key interceptor, in-memory cookie jar), TheDenApi (Retrofit + kotlinx-serialization,
  incl. libraryMovies()/librarySeries() returning JsonObject lists), ErrorSupport.
- ui/theme/: HoltTokens.kt (generated from the-den design/exports), Theme.kt (TheDenTheme, Nunito,
  JetBrainsMono bundled in res/font).
- ui/holt/: Components.kt (Ground, GlassPanel, Badge, Eyebrow, Meta, PageTitle, HoltButton, Chip,
  HoltTextField, HoltRow, StatusBanner, EmptyState, RingMark, Skeleton, ProgressBar, Avatar), Poster.kt
  (posterUrl with ?api_key= for the Plex thumb proxy, PosterCard, Rail).
- vm/: Common.kt (ListViewModel<T> base, seq guard), SessionViewModel, DiscoverViewModels,
  RequestsViewModel, LibraryViewModels, AdminViewModels.
- ui/screens/: LoginScreen, DiscoverScreen (+SearchScreen), DetailScreen (season bottom sheet),
  RequestsScreen, LibraryScreens (Library tabs / Episodes / Candidates / Calendar), AdminScreens
  (Downloads / Indexers / Settings / More).
- MainActivity.kt: NavigationBar on phones, NavigationRail >= 600dp, tabs Discover/Requests/Library/More,
  admin pages behind More, NavHost routes, login gating.
- app/src/test/.../LiveBackendTest.kt: view models against a live backend (DEN_URL, DEN_API_TOKEN).
NOT yet done:
- (DONE 2026-09-12) First compile: assembleDebug BUILD SUCCESSFUL, APK at app/build/outputs/apk/debug/app-debug.apk.
- (DONE 2026-09-12) testDebugUnitTest: LiveBackendTest 5/5 pass against the dev backend (owner token).
- (DONE 2026-09-12) Emulator walk (AVD den_u5, Pixel 7, API 35, headless; script scratchpad/u5/walk.sh): 12 screens, no crashes.
  Found and fixed 3 layout defects (Requests action row -> FlowRow, Detail header aligned to backdrop bottom, TopBar
  double status-bar padding removed); re-verified on the emulator, still no crashes.
  Note: emulator must be launched with setsid/nohup from WSL or it dies with the shell; app reaches the backend at http://10.0.2.2:8686;
  seed prefs via adb run-as com.holtos.theden shared_prefs/the-den.xml (baseUrl, apiToken).
- (DONE 2026-09-12) Android README/STATUS/ROADMAP/HANDOFF updated; U5 row in the-den docs/ui-redesign-plan.md marked done.
- (DONE 2026-09-12) git identity set in the Android repo (Liam / jamesyoungdahr@gmail.com).
- Emulator + system-images;android-35;google_apis;x86_64 installed in /root/android-sdk (WSL).
- (DONE 2026-09-12) Committed 62134f4, tagged v0.5.0, pushed to GitHub. U5 COMPLETE.

BUILD / DEV ENVIRONMENT
-----------------------
- Everything runs in WSL Ubuntu (root). No Python on Windows. WSL was wiped once (2026-09-12)
  and rebuilt: JDK 17 (apt), Android SDK at /root/android-sdk (cmdline-tools, platform 35,
  build-tools 35.0.0), the-den venv, PySide6 venv at ~/.venvs/kde-client (pip; no Kirigami, so
  only the model tests run here), nodriver + playwright Chromium + google-chrome-stable + xvfb
  for solver experiments. Test backend: port 8687, AUTH_REQUIRED=false, DB ~/the-den-test/den.db.
  Dev backend on 8686 reachable from the LAN via a Windows portproxy to the WSL IP (re-add
  after WSL restarts; the WSL IP changes). Phones connect over wireless adb (adb.exe in
  C:SERSiam	oolsplatform-tools); re-pair from the phone's wireless debugging screen.
- the-den venv: ~/.venvs/the-den (apt libtorrent). Dev backend: 127.0.0.1:8686 with AUTH_REQUIRED=true.
  Mock Plex on 8084 via the scratchpad script m11c/start_dev_plex.sh (never put the word "uvicorn" in
  the outer wsl command line). Owner token: ~/the-den-dev/owner-token.txt; owner cookies
  ~/the-den-dev/v-owner.txt. E2E: scratchpad/m11g/full.sh (72 checks; kills the dev stack, restart after).
- KDE client: system PySide6 6.10.2 + Kirigami 6.24; offscreen harness + screenshot walk in the client repo.
- Android: JDK 17 at /usr/lib/jvm/java-17-openjdk-amd64, SDK at /root/android-sdk (platform 35,
  build-tools 35.0.0). Build from /mnt/c/projects/the-den-android:
    export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64 ANDROID_HOME=/root/android-sdk
    ./gradlew --no-daemon -q assembleDebug -Dorg.gradle.vfs.watch=false
    ./gradlew --no-daemon -q testDebugUnitTest -Dorg.gradle.vfs.watch=false
  local.properties (sdk.dir=/root/android-sdk) is gitignored. gradlew must be LF (sed -i 's/\r$//').

RECENTLY COMPLETED MILESTONES
-----------------------------
- 0.4.3 (both repos): library pages on web + KDE client now show Plex-only titles (library_service
  merged_movies/merged_series, /api/library/movies|series, /api/plex/thumb/{rating_key} proxy, PlexMedia.thumb
  column + migration b8c9d0e1f2a3). Web sidebar scrolls on short windows. Verified e2e 72/72.
- 0.4.2: repos split back apart after a brief merge; separate PKGBUILDs.
- U4: HoltOS Glass redesign of the KDE client (13 pages, offscreen screenshot walk).
- M11g: request quotas, "available" detection, sign-in toggle.

RULES AND DECISIONS TO REMEMBER
-------------------------------
- Liam, 2026-09-13 evening: the local models write ALL text in these repos (code, docs and this file). Claude
  plans, writes specs, reviews each real diff, tests and commits. Security-sensitive code is no exception; Claude
  reviews it line by line. If a unit fails twice on both GPUs, ask Liam instead of writing it.
- Liam, 2026-09-13 evening: push only when Liam says so, or as a tagged test release for the HoltOS updater (see
  PICK UP HERE for the lettered version scheme). This replaces the push-and-tag rule below.
- Claude never types API keys/tokens into fields or files. The user pasted the TMDB key into
  app/config.py themselves and approved committing it (non-commercial use).
- Plex owner token stays server-side. The thumb proxy accepts ?api_key= on purpose because <img>
  and QML Image cannot send headers.
- Push to GitHub and tag a release when a milestone lands; the user says "push and tag".
- Keep this file (CONTEXT.txt) updated after EVERY completed to-do item, and before context compaction.
- Liam, 2026-09-13: work straight down the open list without asking for confirmation between items.
  Still ask before destructive or shared-state actions no standing rule covers; tagging still waits for "push and tag".

STILL OPEN / LATER
------------------
- Real-desktop pass of the KDE client on HoltOS.
- Nothing has been tested against real plex.tv (only the mock).
- (Done 2026-09-13) The in-app Browser pane viewport is back on the desktop preset.
